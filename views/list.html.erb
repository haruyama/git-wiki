<% if @pages.empty? %>
  <h1>No pages yet</h1>
<% else %>
  <h1>All pages</h1>

  <div class="content">
    <ul>
      <% @pages.each do |page| %>
        <li>
          <strong><a href="/<%= h page.uri_encoded_name %>"><%= h page.name %></a></strong> &mdash;
          <a href="/e/<%= h page.uri_encoded_name %>">edit</a> &bull;
          <a href="/h/<%= h page.uri_encoded_name %>">history</a> &bull;
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
